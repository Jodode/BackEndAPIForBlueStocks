# BackEndAPIForBlueStocks

Чтобы загрузить БД в postgresql [тык](https://www.pgadmin.org/docs/pgadmin4/development/backup_and_restore.html)

Код для запуска
```
pip install -r requirements.txt
cd app && uvicorn main:app --reload
```
